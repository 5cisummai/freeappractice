diff --git a/dist/bits/utilities/dismissible-layer/use-dismissable-layer.svelte.js b/dist/bits/utilities/dismissible-layer/use-dismissable-layer.svelte.js
index 924869bdb3eedccf57345314adb7c1a9b2a12b20..0e7b4761489f37a91fd6b406c8732dc53e9469c2 100644
--- a/dist/bits/utilities/dismissible-layer/use-dismissable-layer.svelte.js
+++ b/dist/bits/utilities/dismissible-layer/use-dismissable-layer.svelte.js
@@ -33,7 +33,20 @@ export class DismissibleLayerState {
             this.#documentObj = getOwnerDocument(this.opts.ref.current);
         });
         let unsubEvents = noop;
+        // Track the deferred afterSleep timer so teardown can cancel it. Without this,
+        // a layer destroyed within the 1ms window still fires the callback and reads a
+        // destroyed $derived (`ref.current`) → `derived_inert` (and can re-attach document
+        // listeners to a dead instance). See https://github.com/huntabyte/bits-ui/issues/2080
+        let pendingTimer = null;
+        let destroyed = false;
+        const clearPendingTimer = () => {
+            if (pendingTimer != null) {
+                clearTimeout(pendingTimer);
+                pendingTimer = null;
+            }
+        };
         const cleanup = () => {
+            clearPendingTimer();
             this.#resetState();
             globalThis.bitsDismissableLayers.delete(this);
             this.#handleInteractOutside.destroy();
@@ -42,8 +55,11 @@ export class DismissibleLayerState {
         watch([() => this.opts.enabled.current, () => this.opts.ref.current], () => {
             if (!this.opts.enabled.current || !this.opts.ref.current)
                 return;
-            afterSleep(1, () => {
-                if (!this.opts.ref.current)
+            clearPendingTimer();
+            pendingTimer = afterSleep(1, () => {
+                pendingTimer = null;
+                // `destroyed` must short-circuit before any `this.opts.ref.current` read.
+                if (destroyed || !this.opts.ref.current)
                     return;
                 globalThis.bitsDismissableLayers.set(this, this.#behaviorType);
                 unsubEvents();
@@ -52,6 +68,8 @@ export class DismissibleLayerState {
             return cleanup;
         });
         onDestroyEffect(() => {
+            destroyed = true;
+            clearPendingTimer();
             this.#resetState.destroy();
             globalThis.bitsDismissableLayers.delete(this);
             this.#handleInteractOutside.destroy();
