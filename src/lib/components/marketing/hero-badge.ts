/** Static hero eyebrow badge — matches landing page pill style without link hover. */
export const HERO_BADGE_CLASS =
	'h-auto rounded-full border-transparent bg-primary/10 px-2 py-1.5 text-xs leading-none font-medium tracking-tight text-primary/80 backdrop-blur-md sm:px-5 sm:py-2 sm:text-sm dark:bg-primary/15';

/** Hero eyebrow badge when rendered as a link. */
export const HERO_BADGE_LINK_CLASS = `${HERO_BADGE_CLASS} hover:bg-primary/20 hover:text-primary`;

/** Compact pill badge for blog cards and similar meta tags. */
export const HERO_BADGE_COMPACT_CLASS =
	'h-auto rounded-full border-transparent bg-primary/10 px-2.5 py-0.5 text-xs leading-none font-medium tracking-tight text-primary/80 backdrop-blur-md dark:bg-primary/15';
