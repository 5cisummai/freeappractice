/** Root layout server data (flags, assistant availability). */
export const ROOT_LAYOUT_DEPENDENCY = 'fap:root-layout';

/** Authenticated app shell data (orgs, billing, coach sidebar). */
export const APP_LAYOUT_DEPENDENCY = 'fap:app-layout';
